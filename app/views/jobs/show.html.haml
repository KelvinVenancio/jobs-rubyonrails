%h1= @job.title
%p= @job.description
%p= @job.company

= link_to "Home", root_path
= link_to "Editar", edit_job_path(@job)
= link_to "Deletar", job_path(@job), method: :delete, data: { confirm: "Você está certo?" }